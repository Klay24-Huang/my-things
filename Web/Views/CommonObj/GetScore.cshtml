@*@model IEnumerable<Domain.TB.BackEnd.BE_MemScore>

@{
    Layout = null;
    //string aa = "";
    List<SelectListItem> list = new List<SelectListItem>();
}
@if (Model != null)
{
    foreach (var UserGroup in Model)
    {
        list.Add(new SelectListItem() { Text = UserGroup.SCORE.ToString(), Value = UserGroup.SCORE.ToString() });
    }
}

@Html.Text("XXXX", list, new { Class = "form-control" })*@